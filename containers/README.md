mini-container (Go)

A tiny container runtime written in Go.
Inspired by Liz Rice’s “Containers From Scratch” talk.
The goal is to show how containers actually work under the hood — no Docker, no magic, just Linux namespaces + cgroups + exec.